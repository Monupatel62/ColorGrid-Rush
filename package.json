{
  "name": "colorgrid-rush",
  "version": "1.0.0",
  "private": true,
  "description": "ColorGrid Rush - Casual Match-3 Puzzle Game for Web and Android",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build": "npm run build && cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.6.3",
    "vite": "^6.0.1",
    "vitest": "^2.1.8"
  }
}
